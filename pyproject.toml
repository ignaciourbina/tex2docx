[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "doctex"
version = "0.1.0"
description = "Bidirectional LaTeX-to-DOCX converter for academic papers"
requires-python = ">=3.12"
dependencies = [
    "pylatexenc>=3.0a30",
    "python-docx>=1.1.0",
    "click>=8.0",
    "Pillow>=10.0",
    "latex2mathml",
    "lxml",
]

[project.scripts]
doctex = "doctex.cli:cli"

[tool.hatch.build.targets.wheel]
packages = ["src/doctex"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov",
]
